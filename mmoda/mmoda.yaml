# TEMPLATE - move this file to the REPOSITORY ROOT at submission time.
#
# MMODA looks for notebooks at the repository root by default. This tells it to
# look inside the mmoda/ folder instead, and to treat only the utr2_* notebooks
# as services. Notebooks named test_*.ipynb are excluded automatically.
notebook_path: "mmoda"
filename_pattern: "utr2_.*"
