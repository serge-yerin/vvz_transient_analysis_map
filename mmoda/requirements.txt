# Python dependencies for the MMODA service container.
#
# NOTE: MMODA reads `requirements.txt` from the REPOSITORY ROOT, not from a
# subfolder. At submission time this file must be merged into the root
# requirements.txt (see mmoda/README.md section 6). It is kept here for now so
# the desktop application's own requirements.txt stays untouched.

# Installs this repository so that `import src.query` works inside the
# container regardless of the working directory papermill is launched from.
-e .

numpy>=1.26
pandas>=2.1
matplotlib>=3.8
pillow>=10.0
astropy>=6.0

# Deliberately NOT listed: oda-api.
#
# The notebook does not import it - it is only needed for ProgressReporter,
# token access or renku secrets, none of which this workflow uses. Adding it
# pulls in 36 extra packages (17 -> 53), including scipy, bokeh, astroquery,
# pyvo, rdflib, keyring and tornado. Because MMODA reads requirements.txt from
# the repository root, that cost would land on the desktop application too.
# Add it only when a feature actually needs it.
